mixin subscription({ value, placeholder })
  +b.FORM.subscription
    +e.BUTTON.button
      +e.I.icon.material-icons arrow_forward

    +b.INPUT.text-field._padding-right(
      value=value,
      placeholder=placeholder,
      name="subscription",
      type="email"
    )&attributes(attributes)
