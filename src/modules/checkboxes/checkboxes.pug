mixin checkboxes(title, items)
  +b.SECTION.checkboxes
    +e.title
      +b.H2.title_size-3= title

    +e.container
      each itemConfig in items
        - const { text, checked } = itemConfig;

        +e.checkbox-item
        +checkbox-item(text)(checked=checked)
