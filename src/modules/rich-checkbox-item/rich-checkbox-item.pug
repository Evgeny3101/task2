mixin rich-checkbox-item(title, text)
  +b.LABEL.rich-checkbox-item
    +e.INPUT.input(type='checkbox')&attributes(attributes)
    +e.box
    +e.container
      +e.title
        +b.EM.title_normal= title
      +e.text= text
