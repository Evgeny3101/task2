include ../text-field/text-field

mixin dropdown({ type, placeholder, value })
  +b.dropdown
    +text-field(type, placeholder, value)&attributes(attributes)
    +e.I.icon.material-icons keyboard_arrow_down
