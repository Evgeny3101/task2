include ../dropdown/dropdown

mixin menu-count({title, placeholder, value}={})
  +b.SECTION.menu-count
    if (title)
      +e.title
        +b.H2.title_size-3= title

    +e.text-field.js-menu-count
      +dropdown({title, placeholder, value, isParagraph: true}).js-menu-count__text
