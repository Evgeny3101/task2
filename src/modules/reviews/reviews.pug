mixin reviews(img, name, date, text, number, checked)

  +b.reviews
    +e.img-container
      +e.IMG(src=img).img

    +e.name= name
      +e.date= date

    +e.like-button
      +like-button(number, checked)

    +e.text= text